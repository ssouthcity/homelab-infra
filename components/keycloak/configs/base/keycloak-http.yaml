apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
spec:
  parentRefs:
    - name: http
      namespace: kgateway
  hostnames:
    - keycloak.${CLUSTER_DOMAIN}
  rules:
    - backendRefs:
        - name: keycloak-service
          port: 8080
