apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: descheduler
spec:
  interval: 1h
  serviceAccountName: flux
  url: oci://ghcr.io/ssouthcity/mirror/charts/descheduler
  ref:
    tag: 0.36.0 # {"$imagepolicy": "updates-infra:descheduler:tag"}
    digest: sha256:97c9f05dba4244f690719b5525c2a867c35ad3e7a036cd1904a3577e4924afb2 # {"$imagepolicy": "updates-infra:descheduler:digest"}
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: extract
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: descheduler
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: descheduler
  driftDetection:
    mode: enabled
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  values:
    kind: Deployment
    service:
      enabled: true
    serviceMonitor:
      enabled: true
